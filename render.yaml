services:
  - type: web
    name: video-grabber
    runtime: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
    healthCheckPath: /api/health
